mdp

const int N;

module main
   x : [0..N] init 3;
   y : [0..N] init 3;

   [north] x > 1 -> 1:(x'=x-1);
   [south] x < N -> 1:(x'=x+1);
   [west] y > 1 -> 1:(y'=y-1);
   [east] y < N -> 1:(y'=y+1);
endmodule
